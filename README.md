# nomulasuveeksha-reddy-taxcalc-config
GitOps configuration repository

## Day Log

- **w6d3** — CloudFormation stacks for taxcalc dev substrate: `cfn/taxcalc-network-dev.yaml` (3-AZ VPC, IGW, public + private subnets, `SingleNatForDev` condition for cost-aware NAT count), `cfn/taxcalc-app-dev.yaml` (RDS PostgreSQL 16 with Secrets Manager credentials, DB subnet group, security group), `cfn/taxcalc-artifacts-dev.yaml` (ECR repo, S3 artifact bucket), and `cfn/taxcalc-bootstrap-dev.yaml` (OIDC provider, IAM deploy role). `.github/workflows/cfn-validate.yml` runs `cfn-lint`, `cfn-nag`, and `aws cloudformation validate-template` on all stacks as a CI gate.
- **w6d5** — SRE capstone — KEDA ScaledObject on Kafka lag + SLO-derived HPA + PDB + author-and-defend AWS pack: `taxcalc-api/taxcalc-worker.deployment.yaml` is the KEDA scale target (same image as taxcalc-api, `SPRING_PROFILES_ACTIVE=worker`, `replicas: 0`); `taxcalc-api/taxcalc-worker-scaledobject.yaml` binds KEDA to the `taxpayers.events` Kafka topic on consumer group `taxcalc-read-model-builder` (`lagThreshold: "10"`, `minReplicaCount: 0`, `maxReplicaCount: 20`, plaintext broker so no `TriggerAuthentication`). `taxcalc-api/prometheus-adapter-values.yaml` maps the `taxcalc_inflight_requests` Micrometer gauge (added in the app repo) into the custom-metrics API via a single `rules.custom` entry pointed at the W5 D5 kube-prometheus-stack Prometheus Service. `taxcalc-api/hpa.yaml` (`autoscaling/v2`) targets that custom metric with `AverageValue: 6` — derived from single-replica saturation at the 500 ms p99 SLO — with `scaleDown.stabilizationWindowSeconds: 600` lockstepped with Karpenter consolidation. `taxcalc-api/pdb.yaml` sets `minAvailable: 2` so voluntary disruptions never floor the service below the HPA minimum. `taxcalc-api/aws-authored/karpenter-nodepool.yaml` defines a Spot+On-Demand NodePool across three AZs with `WhenEmptyOrUnderutilized` consolidation and fleet limits (200 vCPU / 400 Gi) to cap runaway HPA blast radius. `taxcalc-api/aws-authored/adot-collector.yaml` dual-exports OTLP traces to Tempo (runs on k3d) and X-Ray (AWS-only), keeping the app OTel SDK vendor-neutral. `taxcalc-api/aws-authored/cfn/taxcalc-observability-dev.yaml` is an `AWS::XRay::SamplingRule` with `ReservoirSize: 10` + `FixedRate: 0.05`. `taxcalc-api/aws-authored/taxcalc-worker-scaledobject.sqs.yaml` is the production SQS variant of Task 1's Kafka trigger with `identityOwner: operator` IRSA credential discipline.

- **w6d4** — Cost governance stack + cost-allocation tags on D3 substrate: `cfn/taxcalc-cost-dev.yaml` adds an AWS Budget (`taxcalc-monthly-cost-dev`, COST/MONTHLY, $100 limit, `CostFilters: TagKeyValue: user:service$taxcalc + user:env$dev`, FORECASTED>80% + ACTUAL>100% → SNS), a CloudWatch billing alarm (`taxcalc/estimated-charges-dev`, `AWS/Billing EstimatedCharges`, `TreatMissingData: ignore`, threshold $120), and an SNS topic `taxcalc-cost-alarms-dev` with a `TopicPolicy` granting publish to `budgets.amazonaws.com` and `cloudwatch.amazonaws.com` (SSE via `alias/aws/sns`); `cfn/taxcalc-network-dev.yaml` gains four cost-allocation tags (`service/env/tenant/feature`) on `NatGatewayA/B/C` and `NatEipA/B/C`; `cfn/taxcalc-app-dev.yaml` gains the same four tags on `DbInstance`; `.github/workflows/cfn-validate.yml` is extended with a `validate-template (cost)` step; `cfn-lint cfn/taxcalc-cost-dev.yaml` exits zero; `grep notBreaching cfn/` returns zero.

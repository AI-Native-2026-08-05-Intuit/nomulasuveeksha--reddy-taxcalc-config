# nomulasuveeksha-reddy-taxcalc-config
GitOps configuration repository

## Day Log

- **w6d3** — CloudFormation stacks for taxcalc dev substrate: `cfn/taxcalc-network-dev.yaml` (3-AZ VPC, IGW, public + private subnets, `SingleNatForDev` condition for cost-aware NAT count), `cfn/taxcalc-app-dev.yaml` (RDS PostgreSQL 16 with Secrets Manager credentials, DB subnet group, security group), `cfn/taxcalc-artifacts-dev.yaml` (ECR repo, S3 artifact bucket), and `cfn/taxcalc-bootstrap-dev.yaml` (OIDC provider, IAM deploy role). `.github/workflows/cfn-validate.yml` runs `cfn-lint`, `cfn-nag`, and `aws cloudformation validate-template` on all stacks as a CI gate.
- **w6d4** — Cost governance stack + cost-allocation tags on D3 substrate: `cfn/taxcalc-cost-dev.yaml` adds an AWS Budget (`taxcalc-monthly-cost-dev`, COST/MONTHLY, $100 limit, `CostFilters: TagKeyValue: user:service$taxcalc + user:env$dev`, FORECASTED>80% + ACTUAL>100% → SNS), a CloudWatch billing alarm (`taxcalc/estimated-charges-dev`, `AWS/Billing EstimatedCharges`, `TreatMissingData: ignore`, threshold $120), and an SNS topic `taxcalc-cost-alarms-dev` with a `TopicPolicy` granting publish to `budgets.amazonaws.com` and `cloudwatch.amazonaws.com` (SSE via `alias/aws/sns`); `cfn/taxcalc-network-dev.yaml` gains four cost-allocation tags (`service/env/tenant/feature`) on `NatGatewayA/B/C` and `NatEipA/B/C`; `cfn/taxcalc-app-dev.yaml` gains the same four tags on `DbInstance`; `.github/workflows/cfn-validate.yml` is extended with a `validate-template (cost)` step; `cfn-lint cfn/taxcalc-cost-dev.yaml` exits zero; `grep notBreaching cfn/` returns zero.

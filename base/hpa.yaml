# taxcalc-api/hpa.yaml
# TASK 2 (RUNNABLE): HPA on the custom Prometheus metric
# taxcalc_inflight_requests, fed via the Prometheus Adapter. Target
# 6 comes from a single-replica saturation test (ramp one pod to
# the 500 ms p99 SLO). CPU-only HPA is blind to LLM-API-bound
# latency -- the api pod waits on the network, CPU stays low while
# p99 climbs. Do not also set spec.replicas on the Deployment.
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: taxcalc-api-hpa
  namespace: taxcalc-dev
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: taxcalc-api
  minReplicas: 2
  maxReplicas: 20
  metrics:
    - type: Pods
      pods:
        metric:
          name: taxcalc_inflight_requests
        target:
          type: AverageValue
          averageValue: "6"
  behavior:
    scaleDown:
      stabilizationWindowSeconds: 600   # W5 D3 callback: outlast a bursty dip
      policies: [{type: Percent, value: 25, periodSeconds: 60}]
    scaleUp:
      stabilizationWindowSeconds: 0
      policies: [{type: Percent, value: 100, periodSeconds: 60}]
      selectPolicy: Max
